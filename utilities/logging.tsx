import { logger } from "react-native-logs";

// put this in its own class so we can later apply configs
// found here https://github.com/onubo/react-native-logs

export const logging = logger.createLogger();